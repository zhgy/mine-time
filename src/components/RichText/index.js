import React from 'react';

const RichText = props => {
    return (
        <p>One of the many great parts of React is how it makes you think about apps as you build them. In this document, we’ll walk you through the thought process of building a searchable product data table using React.More</p>
    );
}

export { RichText };